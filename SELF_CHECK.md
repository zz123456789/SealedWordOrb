# 缄言珠自查报告

检查日期：2026-09-21。目标环境：Minecraft 1.20.1、Forge 47.4.10、Java 17。

## 结论

模组 1.0.0 构建和重映射成功，17 项 Minecraft GameTest 服务端集成测试全部通过。已将封存珠改为一次性。除下述已明确接受的纯 NBT 伪造风险外，当前代码审查和自动测试未发现其他明确的功能缺陷。

这不等于已证明没有任何 bug：本次没有启动图形客户端进行实际点击和输入法验证，也没有进行两台真实客户端的联机测试。客户端界面已经编译，并对照 Forge/Minecraft 源码检查了界面注册、输入焦点、关闭行为和最小 320×240 布局。

FTB Quests 1.20.1 的完整物品 NBT 存取及副本奖励分发已核对官方源码，匹配格式的 SNBT 导出/重读测试通过。环境未安装 FTB Quests，因此尚未测试具体 FTB 发行版的领取界面或完整整合包；范围和分发注意事项见 `FTB_REWARDS.md`。

## 自动验证

执行命令：`gradlew.bat build runGameTestServer --offline`。

- 构建与测试日志：`build/verification-1.1.0.log`（一次性实现的原开发编号日志），结果 `BUILD SUCCESSFUL`、`All 17 required tests passed :)`。
- 发布文件：`build/libs/sealedwordorb-1.0.0.jar`，32336 字节。
- SHA-256：`10819E0925770043E39ABCA0BA0D92B3177382A476D3C2099C8549B4A22B1691`。

| 测试 | 结果 |
| --- | --- |
| 指令、默认权限 2、空描述和封存标记经过物品 NBT 保存/读取保持一致 | 通过 |
| 空指令、只有斜杠、超长指令、换行/NUL、非法权限和超长描述不会部分写入 | 通过 |
| 权限 0–4、中文描述正确存取 | 通过 |
| 重复封存被拒绝；Modified 即使为 false 或不同类型也锁定编辑 | 通过 |
| 编辑必须同时满足创造模式和至少 2 级权限 | 通过 |
| 更换手持物品或试图写入高于自身的权限会被拒绝 | 通过 |
| 切换主手槽位、撤销管理员权限使菜单失效，副手受相同保护 | 通过 |
| 生存模式非管理员可以使用已封存珠，@s 指向当前使用者 | 通过 |
| 低权限珠不会借用持有者的管理员权限执行高权限命令 | 通过 |
| 普通和精确实体交互事件会取消实体原交互，各次事件只执行一次 | 通过 |
| 方块交互入口执行一次并消耗此次操作 | 通过 |
| 菜单保存默认值、空描述无 tooltip、损坏封存数据拒绝执行且不重新编辑 | 通过 |
| 新物品和打开菜单不写标签；没有 Modified 的不完整数据仍为默认可编辑状态 | 通过 |
| 经过原版 useItem 入口，创造/生存模式的主副手都消费珠子，不会重复领取 | 通过 |
| 指令替换手持物品后，新奖励及其数量不会被旧珠覆盖或误删 | 通过 |
| FTB 物品奖励格式 SNBT 重读，向新玩家及另一维度分发独立副本，模板保持完整 | 通过 |
| 合法封存珠触发不存在的命令时依然消耗一次使用机会 | 通过 |

实体和方块测试验证服务端事件/方法入口；真实客户端的一次鼠标点击产生哪些网络包尚未实机验证，未将这项等同于真人联机测试。

## 已接受、不修改的问题

**纯 NBT 可被恶意创造客户端伪造。** Minecraft 的创造模式物品提交接口允许任意自定义 NBT；因此恶意客户端可直接写入高权限指令，绕过本模组编辑界面的限制。外部 NBT 工具也能移除 Modified 标记。

曾提出“服务端签名校验，珠子仅限原存档使用”的修复方案。用户明确选择“保持纯 NBT，接受此风险并允许跨存档使用”，本版本据此没有签名、白名单或存档绑定。正常客户端的编辑限制和自定义保存请求的服务端校验仍然有效。

## 留给用户决定的行为修改

1. **保存时不检查指令语法。** 当前按要求只保证指令非空和符合长度/单行约束。输错但非空的指令可以封存，之后无法通过界面改正，执行时由原版报告错误并消耗珠子。可选择增加语法预检查；但实体、维度或其他运行条件仍只能在执行时判断。
2. **失败消耗策略。** 当前采用一次指令尝试即消费，语法/权限/目标错误仍消费；NBT 结构损坏则不执行也不消费。若希望失败时保留，可另行修改，但有些命令失败前已产生部分效果，不能仅根据返回值安全地回滚整个奖励。
3. **没有额外冷却。** 单颗珠子已经一次性；如果手中随后又出现另一颗珠子，持续右键可能使用下一颗，可选择增加冷却时间。

## 实现中已处理的细节

- 保存操作在服务端进行，校验菜单编号、物品身份、手和快捷栏位置，重复包不会覆盖已封存珠。
- 每个字段先验证后统一写入，失败不会留下半封存数据；取消不写 NBT。
- 指令仅在服务端执行；精确替换命令源权限，保留当前玩家身份、位置、朝向和维度。
- 客户端类通过客户端专属事件注册，服务端测试没有触发客户端类加载崩溃。
- 单独隔离测试源码和测试结构，发布 JAR 不包含 GameTest 测试。
- 使用原始贴图；中英文资源齐全；资源过滤固定 UTF-8，避免 Windows 构建时中文模组名乱码。
- 空气右键在原版创造模式数量恢复之前拦截，避免创造模式无限使用；先消费旧珠再执行命令，避免覆盖 `/give` 或 `/item replace` 生成的新物品。
- 保持旧版 NBT 和物品 ID 不变，旧珠子和已有任务奖励升级后直接沿用，但使用规则变为一次性。

## 发布编号修正

一次性实现由原开发编号 1.1.0 修正为 1.0.0；原可重复使用实现由 1.0.0 修正为 0.1.0。行为没有在此次编号修正中变更。当前发布构建日志为 build/publishing/build-1.0.0.log。
